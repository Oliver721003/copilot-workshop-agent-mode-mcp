# 待辦清單 Web App（GitHub Copilot 實戰工作坊）

這是一個在「GitHub Copilot 實戰工作坊」期間完成的練習作品：一個簡潔的待辦清單（To‑Do）網頁應用，示範以原生前端技術快速構建可用且易於擴充的介面。

## 線上展示

示範網址（佔位，請自行替換為你的 Pages 網址）：

https://<你的帳號>.github.io/<你的repo名稱>/

## 功能

- 新增待辦：輸入框 + 新增按鈕，避免新增空白項目。
- 標記完成：每筆事項左側有勾選框，完成後文字加上刪除線並淡化顯示。
- 刪除單筆：每筆事項右側有刪除按鈕，可以移除該筆。
- 未完成計數：頁面底部顯示「未完成: N 項」，數字顯示整體未完成數量（不受篩選影響）。
- 空清單提示：當清單為空顯示「還沒有任何待辦事項,新增一個吧!」。
- 資料持久化：使用 `localStorage` 儲存待辦資料，重新整理後資料保留。
- 深色模式：標題列右側有主題切換按鈕，支援淺/深模式並會記住使用者偏好，若未設定則跟隨作業系統 `prefers-color-scheme`。
- 篩選功能：輸入框下方有三個篩選按鈕（全部 / 未完成 / 已完成），可切換顯示對應項目。
- 篩選提示：在篩選視圖中更動勾選狀態時，若該項目被目前篩選隱藏，會顯示短暫的狀態提示（提醒項目未被刪除）。
- 一鍵清除已完成：在 footer 提供「清除已完成」按鈕，會以瀏覽器原生 `confirm()` 詢問使用者，確認後一次刪除所有已完成項目。

## 技術

- 使用純 HTML、CSS、原生 JavaScript（無框架、無第三方套件）。
- 不建立 `package.json`、不使用 npm；不引用任何外部 CDN，能離線開啟。
- 使用 CSS 變數（`:root`）管理色彩，支援深色主題覆寫。
- 資料暫存在瀏覽器端的 `localStorage`。

## 開發方式（工作流程簡述）

- 本專案開發以小步迭代方式進行，採用由 Copilot / Agent Mode 協助的互動式工作流程做為輔助：
  - 使用由 `.github/prompts` 指定的任務提示，讓 agent 以預定步驟（讀 issue → 提案 → 分支 → 修改 → PR）來協助完成具體修正。
  - 使用 GitHub MCP 工具在程式內部查詢相關文件或取得 repo issue 資訊以決策（例如查詢深色模式的可及性建議）。
  - 所有變更皆以小型分支提交並開 Pull Request，便於審查與回溯。

（註：開發過程中依專案規範執行：程式註解使用繁體中文、變數與函式以英文 camelCase 命名、避免引入第三方套件。）

## 我學到什麼

- 如何用原生 DOM API（`createElement` / `textContent` / `appendChild`）設計互動式 UI，減少 XSS 風險。
- 使用 CSS 變數集中管理主題色，並以 `body.dark` 覆寫變數以支援深色主題。
- 在 UX 設計上，要處理好「篩選視圖下的狀態回饋」，避免使用者誤以為資料被刪除。
- 把自動化 agent（提示、分支、PR）納入開發流程，可以加速重複性步驟但仍需人工審查。
- 測試色彩對比與可及性（尤其在深色模式下）是不可忽略的一環，應使用瀏覽器開發工具檢查對比問題。

---

如需我協助把作品部署到 GitHub Pages 或把 README/PORTFOLIO 的內容微調成履歷格式，我可以接著協助。
