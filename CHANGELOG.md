# 變更記錄

以下為此待辦清單 App 的功能紀錄（繁體中文）。

## [0.1.0] - Step 1: 基本功能

### 新增

- 新增待辦項目：輸入框 + 新增按鈕
- 標記完成：每一筆待辦左側有勾選框
- 刪除項目：每一筆待辦右側有刪除按鈕
- 未完成計數：底部顯示「未完成: N 項」
- 資料儲存：使用 localStorage 保存，重新整理後資料仍保留

### 變更

- 初始版本發布

## [0.2.0] - Step 2: 深色模式與篩選

### 新增

- 深色模式切換：標題列右側新增切換按鈕，可在淺色 / 深色間切換
  - 按鈕會顯示對應圖示與文字（淺色時顯示「🌙 深色模式」，深色時顯示「☀️ 淺色模式」）
  - 使用者選擇會儲存（記住偏好），重新整理後維持
  - 若使用者未手動切換，預設會跟隨作業系統的 `prefers-color-scheme`
- 篩選功能：在輸入框下方新增三個篩選按鈕（全部 / 未完成 / 已完成），點選會只顯示對應項目

### 變更

- 樣式改為使用 CSS 變數以支援深色主題覆寫
- JavaScript 加入篩選與主題切換邏輯，並更新 DOM 渲染行為
